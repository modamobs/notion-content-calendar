export default {
  plugins: {
    // Tailwind is not used in this client build; keep it disabled to avoid v4 PostCSS plugin errors.
    // If you want Tailwind, install '@tailwindcss/postcss' and enable it here.
    // '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
}